<?php/*
    class Crud
    {
        // Consulta SQL para obtener todos los Jesuitas
    $sql = "SELECT * FROM jesuit";

// Ejecutar la consulta
$result = $conn->query($sql);
    // Verificar si hay resultados
    if ($result->num_rows > 0) {
        // Mostrar los datos en una tabla, por ejemplo
    echo "<table border='1'>
    <tr>
        <th>ID Jesuita</th>
        <th>Nombre</th>
        <th>Firma</th>
    </tr>";

        // Iterar sobre los resultados y mostrarlos en la tabla
    while ($row = $result->fetch_assoc()) {
    echo "<tr>
            <td>" . $row['id'] . "</td>
            <td>" . $row['nombre'] . "</td>
            <td>" . $row['firma'] . "</td>
        </tr>";
    }

echo "</table>";
} else {
    echo "No se encontraron Jesuitas.";
}

    }
*/?>